pipeline {
    agent any 
    stages {
        stage('build') {
            steps {
                dir('vpc1'){
                    sh 'terraform init'
                    sh 'sucess'
                }
            }
        }
    }
}